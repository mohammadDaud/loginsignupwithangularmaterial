import { Accounttransaction } from './accounttransaction';

describe('Accounttransaction', () => {
  it('should create an instance', () => {
    expect(new Accounttransaction()).toBeTruthy();
  });
});
